import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
    {/* self closing tag */}
  </React.StrictMode>,
  document.querySelector("#root")
);
